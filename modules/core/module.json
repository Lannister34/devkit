{
  "name": "core",
  "phase": "foundation",
  "description": "Language-agnostic engineering rules: comments, module seams, failure paths, documentation.",
  "requires": [],
  "detect": { "always": true },
  "conflicts": [],
  "vars": {
    "docLanguage": "English"
  },
  "checks": [
    {
      "type": "doc-language",
      "key": "doc-language",
      "sample": 40,
      "minShare": 0.05
    }
  ],
  "files": [
    { "from": "templates/CLAUDE.md", "to": "CLAUDE.md", "strategy": "section", "marker": "core", "header": "# {{project}}\n\n## Project\n\n<!-- TODO: fill in on first contact — the agent reads this file every session.\nOne paragraph: what this is and why. After that, links rather than retellings (one fact,\none place): architecture and how to run it, domain vocabulary, decisions (ADR). Then:\nprocesses and the cost of their failure (what must never be restarted), check commands.\nKeep your own text OUTSIDE the devkit markers — the toolkit rewrites section contents. -->\n\n" }
  ]
}
