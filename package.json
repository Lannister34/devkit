{
  "name": "devkit",
  "version": "0.1.5",
  "private": true,
  "description": "Portable engineering rules, quality gates, and a Claude Code init skill.",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "detect": "node bin/apply.mjs --detect --target",
    "plan": "node bin/apply.mjs --target"
  }
}
